# 1. 大类资产驱动与建模数据

---

## 1.1 四大类资产建模指标


| 资产类别  | 指标代码                       | Choice ID   | 展示名称                   | 数据来源                   | 周期  | 频率  | 历史窗口           | 定位               |
| ----- | -------------------------- | ----------- | ---------------------- | ---------------------- | --- | --- | -------------- | ---------------- |
| 无风险利率 | CGB_1Y                     | E1000172    | 中债国债到期收益率:1年           | 中债 / Wind / Choice     | 日度  | 日频  | 近15-20年（按可得最长） | 建模无风险利率          |
| 现金类   | MMF_7D_YIELD_M             | EMI01516342 | 中国货币市场基金月平均七日年化收益率     | Choice / 基金业协会 / Wind  | 月度  | 月频  | 2004-01至今      | 方案A现金主腿          |
| 固收类   | CBOND_NEW_COMPOSITE_WEALTH | EMM01590538 | 中债新综合指数（中债-新综合指数-财富指数） | 中债 / Wind / Choice     | 月度  | 月频  | 按可得最长          | 方案A固收主腿 / 固收回测基准 |
| 固收类   | CGB_3Y                     | E1000174    | 中债国债到期收益率:3年           | 中债 / Wind / Choice     | 日度  | 日频  | 近10-15年（按可得最长） | 信用利差分母           |
| 固收类   | AA_CREDIT_YIELD_3Y         | E1000469    | AA级信用债收益率（3年）          | 中债 / Wind / Choice     | 日度  | 日频  | 近10-15年（按可得最长） | 信用风险主锚           |
| 股票类   | CSI300_TR                  | H00300      | 沪深300全收益指数             | 中证指数公司 / Wind / Choice | 日度  | 日频  | 按可得最长          | 方案A股票主腿          |
| 股票类   | CSI300                     | 000300      | 沪深300指数                | 中证指数公司 / Wind / Choice | 日度  | 日频  | 2005年至今        | 方案B股票主腿          |
| 另类资产  | AU9999                     | AU9999      | 上海黄金交易所Au99.99         | 上金所 / Wind / Choice    | 日度  | 日频  | 近20年           | 黄金主腿（A/B通用）      |
| 另类资产  | NHCI                       | NHCI        | 南华商品指数                 | 南华期货 / Wind / Choice   | 日度  | 日频  | 近20年           | 商品主腿             |


---

## 1.2 说明

- 本章仅保留**直接进入四大类资产收益建模或风险建模**的外部原始指标。
- 无风险利率单独补入 `CGB_1Y`，用于组合优化、超额收益计算、夏普比率、Black-Litterman / MVO 等模型中的 `r_f`。
- 现金类在中国市场缺少统一、长期、稳定可得的现金总回报指数，因此当前采用**中国货币市场基金月平均七日年化收益率**作为最稳妥近似。
- 固收类保留一条**总回报口径主腿**与一组**利率 / 信用风险分解指标**，便于同时支持收益回测与信用利差分析。
- 股票类同时保留**总回报口径**与**价格口径**，分别对应长期配置回报建模与价格因子建模。
- 另类资产中，黄金与商品共同承担分散作用；如后续可得商品期货总回报指数，可进一步优化方案 A 的另类商品口径。

---

## 1.3 加工方式（非采购项）

以下加工结果均由上述已采购原始指标在系统内计算，不属于外部采购项：

- `r_f = CGB_1Y`
- `r_cash_A ≈ MMF_7D_YIELD_M / 12`
- `CREDIT_SPREAD_3Y_AA = AA_CREDIT_YIELD_3Y - CGB_3Y`
- `r_equity_A = ln(CSI300_TR_t / CSI300_TR_{t-1})`
- `r_equity_B = ln(CSI300_t / CSI300_{t-1})`
- 另类资产收益率序列由原始价格序列在系统内统一生成；如进入月度回测，可在系统内聚合为月频收益序列。

---

## 1.4 方案 A / 方案 B（简要说明）

> 说明：本节只做简要解释，不改变本章“采购清单以原始指标为主”的定位。四大类资产定义为：现金类强调流动性管理，固收类强调稳健收益，股票类承担长期进攻，另类资产承担分散风险与组合优化。

### 1.4.1 方案 A：Total Return（投资回报口径）

- 无风险利率：`CGB_1Y`
- 现金类：`MMF_7D_YIELD_M`
- 固收类：`CBOND_NEW_COMPOSITE_WEALTH`
- 股票类：`CSI300_TR`
- 另类-黄金：`AU9999`
- 另类-商品：`NHCI`

### 1.4.2 方案 B：Price Factor（价格因子口径）

- 无风险利率：`CGB_1Y`
- 固收利率 / 信用分解：`CGB_3Y`、`AA_CREDIT_YIELD_3Y`
- 股票类：`CSI300`
- 另类-黄金：`AU9999`
- 另类-商品：`NHCI`

---

## 1.5 相关性矩阵、预期收益与无风险利率的多窗口运算规则

### 1.5.1 运算对象与边界

- 本节面向四大类资产配置建模的参数层，统一回答三类核心问题：**相关性矩阵怎么计算、长期预期收益 `μ` 怎么估计、无风险利率 `r_f` 怎么进入模型**。
- 风险资产统一按 **5 条资产腿** 处理：
  - 现金：`MMF_7D_YIELD_M`
  - 固收：`CBOND_NEW_COMPOSITE_WEALTH`
  - 股票：`CSI300_TR`（方案 A 主口径）
  - 黄金：`AU9999`
  - 商品：`NHCI`
- 无风险利率单独采用 `CGB_1Y`，作为 `r_f` 序列处理，**不进入 risky assets 相关性矩阵 / 协方差矩阵**。
- 方案 B 中的 `CGB_3Y`、`AA_CREDIT_YIELD_3Y`、`CSI300` 更适合用于利率冲击、信用利差、价格因子解释，不与方案 A 的资产总回报主腿混合运算同一套配置矩阵。
- 因此系统内建议保留 **两套矩阵体系**：
  - **方案 A：资产配置主矩阵**，用于组合优化、风险预算、回测、Black-Litterman / MVO。
  - **方案 B：解释型因子矩阵**，用于风险归因、市场解释、战术分析。

### 1.5.2 方案 A / 方案 B 的口径与加工方法

为避免总回报口径、价格口径、收益率水平口径混用，本节将方案 A 与方案 B 的运算对象、加工方式与适用场景明确分开。

#### （1）方案 A：Total Return（投资回报口径）

- **定位**：正式配置主矩阵，用于组合优化、风险预算、长期回测、Black-Litterman / MVO。
- **核心原则**：尽可能使用可投资、可持有、可累计的总回报口径；统一转成月频收益率后再计算相关性矩阵、协方差矩阵与长期预期收益 `μ`。
- **统一频率**：月频。
- **统一口径**：月收益率。
- **资产腿定义**：
  - 现金：`MMF_7D_YIELD_M`
  - 固收：`CBOND_NEW_COMPOSITE_WEALTH`
  - 股票：`CSI300_TR`
  - 黄金：`AU9999`
  - 商品：`NHCI`
- **加工方式**：

```text
r_cash_A,t      ≈ (1 + MMF_7D_YIELD_M_t)^(1/12) - 1
r_bond_A,t      = ln(CBOND_NEW_COMPOSITE_WEALTH_t / CBOND_NEW_COMPOSITE_WEALTH_{t-1})
r_equity_A,t    = ln(CSI300_TR_t / CSI300_TR_{t-1})
r_gold_A,t      = ln(AU9999_t / AU9999_{t-1})
r_cmdty_A,t     = ln(NHCI_t / NHCI_{t-1})
```

- **收益矩阵定义**：

```text
R_A,t = [r_cash_A,t, r_bond_A,t, r_equity_A,t, r_gold_A,t, r_cmdty_A,t]
```

- **适用事项**：
  - 相关性矩阵 `Corr_A(W)`
  - 协方差矩阵 `Cov_A(W)`
  - 长期预期收益 `μ_A(W)`
  - 组合优化与历史回测

#### （2）方案 B：Price Factor（价格 / 利率因子口径）

- **定位**：解释型因子矩阵，用于风险归因、市场解释、利率冲击分析、信用利差分析、战术分析。
- **核心原则**：方案 B 不是“可投资组合的总回报矩阵”，而是“价格 / 利率 / 信用因子的联动矩阵”；因此不能与方案 A 混成同一套正式配置矩阵。
- **统一频率**：建议月频。
- **统一口径**：价格类转收益率，利率类与信用类转变化量。
- **因子定义**：
  - 股票价格因子：`CSI300`
  - 利率因子：`CGB_3Y`
  - 信用因子：`AA_CREDIT_YIELD_3Y`
  - 黄金价格因子：`AU9999`
  - 商品价格因子：`NHCI`
- **加工方式**：

```text
r_equity_B,t        = ln(CSI300_t / CSI300_{t-1})
Δy_cgb3y_B,t        = CGB_3Y_t - CGB_3Y_{t-1}
spread_aa3y_t       = AA_CREDIT_YIELD_3Y_t - CGB_3Y_t
Δspread_aa3y_B,t    = spread_aa3y_t - spread_aa3y_{t-1}
r_gold_B,t          = ln(AU9999_t / AU9999_{t-1})
r_cmdty_B,t         = ln(NHCI_t / NHCI_{t-1})
```

- **因子矩阵定义**：

```text
R_B,t = [r_equity_B,t, Δy_cgb3y_B,t, Δspread_aa3y_B,t, r_gold_B,t, r_cmdty_B,t]
```

- **适用事项**：
  - 解释型相关性矩阵 `Corr_B(W)`
  - 解释型协方差矩阵 `Cov_B(W)`
  - 风险传导、市场归因、组合解释

#### （3）边界要求

- 方案 A 与方案 B **不得混用同一套收益矩阵**。
- `CBOND_NEW_COMPOSITE_WEALTH` 属于**总回报口径**，应留在方案 A；`CGB_3Y` 与 `AA_CREDIT_YIELD_3Y` 属于**利率 / 信用因子口径**，应留在方案 B。
- `CSI300_TR` 属于**全收益口径**，用于方案 A；`CSI300` 属于**价格口径**，用于方案 B。
- `AU9999` 与 `NHCI` 虽在两套方案中都可出现，但在方案 A 中代表**资产回报腿**，在方案 B 中代表**价格联动因子**，解释含义不同。
- 因此，正式配置、长期回测、均值-方差优化应优先采用方案 A；解释市场波动来源、拆解利率与信用风险、生成投顾归因话术时再使用方案 B。

### 1.5.3 相关性矩阵与协方差矩阵的计算规则

#### （1）方案 A：正式配置矩阵

- 基于 `R_A,t` 计算正式配置矩阵：

```text
Corr_A(W) = corr(R_A; window = W)
Cov_A(W)  = cov(R_A; window = W)
```

- 若使用月频收益率，则年化协方差矩阵为：

```text
Cov_A,annual(W) = 12 × Cov_A,monthly(W)
```

- 方案 A 的相关性矩阵与协方差矩阵只对 **5 条风险资产腿** 计算，**不包含 `r_f`**。
- 方案 A 输出的是可直接进入配置模型的正式参数，优先级最高。

#### （2）方案 B：解释型因子矩阵

- 基于 `R_B,t` 计算解释型矩阵：

```text
Corr_B(W) = corr(R_B; window = W)
Cov_B(W)  = cov(R_B; window = W)
```

- 方案 B 的矩阵用于解释“股、利率、信用、黄金、商品”之间的联动关系，重点服务于：
  - 市场解释
  - 风险归因
  - 利率冲击分析
  - 信用利差分析

- 方案 B 不直接替代方案 A 的正式配置矩阵，也不直接作为长期配置优化器的主输入。

#### （3）统一校验规则

- 所有进入矩阵的序列必须满足：**同频、同口径、同窗口**。
- 不得把价格 level、收益率 level、总回报指数收益率、信用利差变化量混在同一个矩阵中直接计算。
- 若系统只保留一套“正式配置参数”，则默认应保留：

```text
Corr_A(W)
Cov_A(W)
μ_A(W)
```

- 方案 B 作为解释层并行输出，不替代方案 A。

### 1.5.3 相关性矩阵与协方差矩阵的计算规则

- 方案 A 的正式配置矩阵，基于 `R_t` 直接计算：

```text
Corr(W) = corr(R; window = W)
Cov(W)  = cov(R; window = W)
```

- 其中：
  - `Corr(W)` 为窗口 `W` 下的相关性矩阵；
  - `Cov(W)` 为窗口 `W` 下的协方差矩阵；
  - 若使用月频收益率，则年化协方差矩阵为：

```text
Cov_annual(W) = 12 × Cov_monthly(W)
```

- 相关性矩阵与协方差矩阵只对 **5 条风险资产腿** 计算，**不包含 `r_f`**。
- 正式配置场景优先使用方案 A，不建议把总回报指数、价格指数、收益率水平、信用利差等不同口径序列混入同一套配置矩阵。

### 1.5.4 多窗口设置规则

考虑中国市场与全球市场均存在明显的制度切换与危机阶段，仅保留 36M / 60M / 120M 不足以覆盖 2015 年中国股灾与 2008 年全球金融危机。因此，参数窗口建议分为三层：

#### （1）短中期观察窗口

- `12M`
- `24M`
- `36M`

用途：
- 观察近端 regime 变化；
- 捕捉相关性漂移；
- 支持战术监控与敏感性分析。

#### （2）主配置窗口

- `60M`
- `120M`

用途：
- 作为正式配置、回测、风险预算的主输入；
- 在稳定性与现实性之间取得平衡。

建议：
- `60M` 作为**默认标准窗口**；
- `120M` 作为**中长期锚**。

#### （3）危机校准窗口

- `180M`
- `240M`

用途：
- 覆盖更完整的压力样本；
- 将 2015 年中国股灾、2008 年全球金融危机等危机阶段纳入参数校准；
- 检查当前窗口是否低估极端市场下的共振风险。

### 1.5.5 长期预期收益 `μ` 的多窗口估计规则

- 预期收益 `μ` 可以、也应当按不同窗口分别估计。
- 对于任一窗口 `W`，月频历史均值定义为：

```text
μ(W) = mean(R_t; window = W)
```

- 若需年化，可采用：

```text
μ_annual(W) = 12 × μ_monthly(W)
```

- 系统建议至少保留以下参数：

```text
μ_12M, μ_24M, μ_36M, μ_60M, μ_120M, μ_180M, μ_240M
```

- 各窗口角色建议如下：
  - `μ_12M`、`μ_24M`：反映短期趋势，但噪声较大，只适合做修正参考；
  - `μ_36M`、`μ_60M`：实务基准；
  - `μ_120M`、`μ_180M`、`μ_240M`：长期锚，用于平滑短期噪声与补足危机样本。
- 投顾与配置场景下，不建议机械地只使用单一历史均值作为未来收益预期。更稳妥的做法是保留多窗口 `μ`，并在优化器或上层配置逻辑中进行融合。
- 默认建议：

```text
μ* = 融合(μ_60M, μ_120M)
```

必要时可再结合 `μ_180M` 或 `μ_240M` 做长期校准。

### 1.5.6 无风险利率 `r_f` 的运算规则

- 无风险利率采用 `CGB_1Y`，先统一转换为月频无风险收益率序列：

```text
r_f,t ≈ (1 + CGB_1Y_t)^(1/12) - 1
```

- 若原始数据为百分比口径，需先除以 100 后再计算。
- `r_f` 的角色是：
  - 计算超额收益；
  - 计算夏普比率；
  - 进入 MVO / Black-Litterman / 切点组合等优化器。
- `r_f` **不是相关性矩阵的一部分**，也不单独构造“无风险矩阵”。

#### （1）历史回测中的用法

对任一风险资产 `i`：

```text
r_excess,i,t = r_i,t - r_f,t
```

即按时间点一一对应地从资产收益中扣除当期无风险收益率。

#### （2）长期参数中的用法

可按不同窗口保留平均无风险利率参数：

```text
rf_avg_12M, rf_avg_24M, rf_avg_36M, rf_avg_60M, rf_avg_120M, rf_avg_180M, rf_avg_240M
```

其含义不是“无风险矩阵”，而是：
- 某一窗口下的平均无风险收益率；
- 用于 Sharpe、资本市场线、长期配置比较与优化器输入。

#### （3）默认建议

- 历史回测：使用逐期 `r_f,t`；
- 当前优化器：优先使用 `rf_avg_12M` 或最近时点 `r_f`；
- 长期研究：结合 `rf_avg_60M`、`rf_avg_120M`、`rf_avg_240M` 观察不同利率 regime 下的长期锚。

### 1.5.7 推荐的系统输出对象

围绕每个窗口 `W ∈ {12M, 24M, 36M, 60M, 120M, 180M, 240M}`，系统建议输出四类核心参数：

```text
Corr(W)
Cov(W)
μ(W)
rf_avg(W)
```

其中：
- `Corr(W)`：5 条风险资产腿的相关性矩阵；
- `Cov(W)`：5 条风险资产腿的协方差矩阵；
- `μ(W)`：5 条风险资产腿的预期收益向量；
- `rf_avg(W)`：对应窗口下的平均无风险收益率。

### 1.5.8 默认落地建议

- **正式配置主输入**：
  - `Cov_60M`
  - `μ* = 融合(μ_60M, μ_120M)`
  - `rf_avg_12M` 或最近时点 `r_f`
- **敏感性 / 漂移监控**：
  - `Corr_12M`
  - `Corr_24M`
  - `Corr_36M`
  - 对比 `Corr_60M`
- **危机校准检查**：
  - `Cov_180M`
  - `Cov_240M`

若当前 `60M` 参数明显低于 `180M / 240M` 危机校准窗口所反映的共振强度，则应提示当前配置矩阵可能低估极端市场下的组合风险。

---

## 1.6 一句话总结

> **本章保留的是四大类资产配置建模真正需要采购的外部原始指标：以 `CGB_1Y` 作为无风险利率锚，以货基收益率、债券财富指数、股票指数、黄金与商品指数共同构成可解释、可回测、可落地的四大类资产建模底座。**

